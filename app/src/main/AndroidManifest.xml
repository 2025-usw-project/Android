<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.su.washcall">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme._2gradeproject"
        tools:targetApi="31">

        <!-- 앱의 첫 시작 화면을 LoginActivity로 유지 -->
        <activity android:name=".LoginActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 다른 Activity 선언들 (기존 코드 유지) -->
        <activity android:name=".MachinListActivity" android:exported="false" />
        <activity android:name=".RoomListActivity" android:exported="false" />
        <activity android:name=".RegisterActivity" android:exported="false" />
        <activity android:name=".OnboardingSimpleActivity" android:exported="false" />
        <activity android:name=".OnboardingSimple2Activity" android:exported="false" />
        <activity android:name=".MainActivity" android:exported="false" />
        <activity android:name=".AdminDashboardActivity" android:exported="false" android:label="관리자 대시보드" />
        <activity android:name=".CalibrationActivity" android:exported="false" android:label="캘리브레이션" />
        <activity android:name=".qr.QRScanActivity" android:exported="false" android:screenOrientation="portrait" />
        <activity android:name=".qr.StartScanActivity" android:exported="true" />

        <!-- ===================================================================== -->
        <!-- 사용자 관련 Activity들의 관계를 명확하게 재정의합니다. -->
        <!-- ===================================================================== -->

        <!-- 1. 사용자 대시보드 (부모 없음) -->
        <activity
            android:name=".UserDashboardActivity"
            android:exported="false" />

        <!-- 2. 세탁실 목록 (부모: UserDashboardActivity) -->
        <activity
            android:name=".UserRoomListActivity"
            android:exported="false"
            android:parentActivityName=".UserDashboardActivity" />

        <!-- 3. 세탁기 목록 (부모: UserRoomListActivity) -->
        <!-- ▼▼▼ [핵심 수정] 문제가 되는 tools:theme 라인을 완전히 삭제하여 빌드 오류를 해결합니다. ▼▼▼ -->
        <activity
            android:name=".UserMachineListActivity"
            android:exported="false"
            android:parentActivityName=".UserRoomListActivity" />
        <!-- ▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲ -->


        <!-- ===================================================================== -->

        <service
            android:name=".MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>
